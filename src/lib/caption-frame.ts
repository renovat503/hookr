/** Design canvas for hook captions — preview, PNG capture, and ffmpeg burn all use this. */
export const CAPTION_FRAME = { width: 1080, height: 1920 } as const;

/** Horizontal inset from each hook edge (1080×1920 frame). */
export const CAPTION_SIDE_MARGIN_PX = 15;
